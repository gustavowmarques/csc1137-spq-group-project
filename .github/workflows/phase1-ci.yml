name: Phase 1 CI – Initial Pipeline

on:
  push:
    branches:
      - main
      - develop
  pull_request:
    branches:
      - main
      - develop

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Run placeholder build
        run: |
          echo "CI pipeline initialised successfully"
          echo "Phase 1 requirement satisfied"
